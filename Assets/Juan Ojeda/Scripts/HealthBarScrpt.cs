using UnityEditor.Build;
using UnityEngine;
using UnityEngine.UI;
public class HealthBarScrpt : MonoBehaviour
{
    //[SerializeField] private Slider HealthBarSlider;
    //public Slider HealthBar => HealthBarSlider;
    //[SerializeField] private Slider EaseHealthSlider;
    //public Slider EaseSlider => EaseHealthSlider;
    //[SerializeField] private InventoryBehaviour player;

    //private float lerpSpeed = 15f; 

    //// Start is called once before the first execution of Update after the MonoBehaviour is created
    //void Start()
    //{
    //    HealthBarSlider.maxValue = player.playerLife;
    //    EaseHealthSlider.maxValue = player.playerLife;

    //    HealthBarSlider.value = player.playerLife;
    //    EaseHealthSlider.value = player.playerLife;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    HealthBarSlider.value = player.playerLife;

    //    if (EaseHealthSlider.value != HealthBarSlider.value)
    //    {
    //        EaseHealthSlider.value = Mathf.MoveTowards(EaseHealthSlider.value, HealthBarSlider.value, lerpSpeed * Time.deltaTime);
    //    }

    //}

    
}
